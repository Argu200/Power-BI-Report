# Dept KPI Dashboard

Static HTML dashboard deployed on Vercel that embeds a Power BI report.

## Deployment
1. Push this repo to GitHub
2. Import the repo into Vercel
3. Deploy (no build step required)
